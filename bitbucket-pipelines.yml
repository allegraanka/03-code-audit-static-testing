# Publishes the Storybook instance to Chromatic.

pipelines:
  default:
    - step:
        name: 'Publish to Chromatic'
        cache:
          - node
        script:
          - cd app
          - npm install
          - npm run chromatic --project-token=${CHROMATIC_PROJECT_TOKEN} --build-script-name=storybook-build --output-dir=storybook-static
