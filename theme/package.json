{
  "name": "pavers-theme",
  "scripts": {
    "build": "npx mix",
    "deploy": "yarn build && shopify-themekit deploy -d=dist --ignored-file=config/settings_data.json"
  },
  "devDependencies": {
    "@shopify/themekit": "^1.1.9",
    "browser-sync": "^2.27.5",
    "browser-sync-webpack-plugin": "^2.3.0",
    "laravel-mix": "^6.0.28",
    "resolve-url-loader": "^4.0.0",
    "sass": "^1.38.1",
    "sass-loader": "^12.1.0"
  }
}
