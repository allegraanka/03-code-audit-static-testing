query customerOrders($customerAccessToken: String!) {
  customer(customerAccessToken: $customerAccessToken) {
    orders(first: 100) {
      edges {
        node {
          id
          orderNumber
          fulfillmentStatus
          financialStatus
          processedAt
          totalPriceV2 {
            amount
          }
        }
      }
    }
  }
}
