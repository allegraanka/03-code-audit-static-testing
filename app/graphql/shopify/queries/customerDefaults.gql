query customerDefaults($customerAccessToken: String!) {
  customer(customerAccessToken: $customerAccessToken) {
    orders(first: 1) {
      edges {
        node {
          id
          orderNumber
          fulfillmentStatus
          financialStatus
          processedAt
          totalPriceV2 {
            amount
          }
        }
      }
    }
    defaultAddress {
      formatted
    }
  }
}
